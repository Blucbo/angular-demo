import { SearchNameEmailPipe } from './search-name-email.pipe';

describe('SearchNameEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNameEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
